{% extends 'base.html.twig' %}

{% block title %}Liste recettes{% endblock %}

{% block main %}
    <div class="container">
        <a href="{{ path('app_accueil') }}" class="btn btn-primary mt-4">Retour à l'accueil</a>
        <h1 class="mt-4">Liste des recettes</h1>
        <ul class="container-fluid d-flex flex-column">
            {% for recipe in publicRecipes %}
                <li class="mt-3 border border-4 p-4">
                    <div class="d-flex justify-content-between">
                        <h4 class="bg-warning p-1">{{ recipe.name }}</h4>
                        <span>Créée le {{ recipe.publishedAt|date('d/m/Y') }}</span>
                    </div>
                    <h5 class="mt-3">Description</h5>
                    <p>{{ recipe.description }}</p>
                    <div class="container-fluid d-flex justify-content-end bg-danger">
                        <a href="{{ path('app_recette', {'id' : recipe.id }) }}" class="btn btn-primary w-50 h-50">Consulter</a>
                    </div>
                </li>
            {% endfor %}
        </ul>    
    </div>
    <div class="navigation d-flex justify-content-center mt-4 mb-4">
        {{ knp_pagination_render(publicRecipes) }}
    </div>
{% endblock %}